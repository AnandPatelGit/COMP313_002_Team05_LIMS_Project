<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anandpatelak.lims_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".InstructorActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".CreateFolder" />
        <activity android:name=".UploadFile" />
        <activity android:name=".InstructorFolderItems" />
        <activity android:name=".DownloadFile" />
        <activity android:name=".AddSubjectActivity" />
        <activity android:name=".InstructorAvailableSubjects" />
        <activity android:name=".InstructorFeedbackActivity" />
        <activity android:name=".DisplayFeedbackActivity" />
        <activity android:name=".DisplaySingleFeebackActivity" />
        <activity android:name=".StudentAvailableSubjects" />
        <activity android:name=".StudentFolders"></activity>
    </application>

</manifest>